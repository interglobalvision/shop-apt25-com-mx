<section id="page-{{ page.handle }}">
  <div class="container">
    <div class="grid-row">
      <div class="grid-item item-s-12">
        <h1 class="font-size-mid font-bold margin-bottom-small">{{ page.title }}</h1>
        {{ page.content }}
      </div>
    </div>
  </div>
</div>
